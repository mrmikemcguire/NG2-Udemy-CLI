import { Component } from '@angular/core';

@Component ({
  selector: 'my-input',
  template: `

    `
})

export class InputComponent {
  myself = {name: '', age: ''};
  isFilled = false;
  isValid = false;
}
